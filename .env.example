# Copy this file to ".env" and fill in your real MySQL credentials (same as MySQL Workbench).
# Command: copy .env.example .env   (PowerShell: Copy-Item .env.example .env)

DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
